# DXF + 底图服务集成说明

## 新增文件
- `slave_viewer_map.html` - 带底图服务的增强版查看器

## 功能特性

### 1. 多种底图支持
- **高德地图** - 默认，国内推荐使用
- **天地图** - 国家测绘局官方底图（需要申请 Key）
- **OpenStreetMap** - 国际开源底图
- **无底图** - 纯 DXF 显示（与原 viewer 相同）

### 2. 坐标系支持
- WGS84 (EPSG:4326) - 国际标准
- Web墨卡托 (EPSG:3857) - 在线地图标准
- CGCS2000 系列 - 国家2000坐标系
- 局部坐标 - 支持自定义配准

### 3. DXF 实体支持
全部 `parse` 返回的实体类型：
- AddLine / AddPolyline / AddSpline
- AddCircle / AddArc
- AddEllipse
- AddText / AddMText
- AddInsert (块参照)

### 4. 交互功能
- 鼠标悬停显示坐标（经度/纬度）
- 点击实体选中高亮
- 实体列表点击定位
- 图层开关控制
- 视图缩放/平移/全图
- 导出 GeoJSON / KML
- **坐标配准工具** - 将局部 CAD 坐标对齐到地图

### 5. 坐标配准工具（新增）
当 DXF 使用局部 CAD 坐标时，通过配准工具将其对齐到地图：

**操作步骤：**
1. 上传 DXF 后，左侧面板选择 **局部坐标 (需配准)**
2. 点击 **🔴 开始配准** 按钮进入配准模式
3. **第一步**：在 DXF 图形上点击一个特征点（如建筑拐角）
4. **第二步**：在底图上点击对应的实际位置
5. 重复 2-4 步，添加至少 **2 对**配准点（建议 3 对更精确）
6. 点击 **✓ 应用** 完成配准

**配准原理：**
- 2 个点：平移 + 缩放 + 旋转
- 3 个及以上点：仿射变换（更精确的配准）

**配准点显示：**
- 🔴 红色标记：DXF 上的点
- 🟢 绿色标记：地图上的对应点
- 配对点用数字标识

## 使用方法

### 方式一：直接打开（推荐测试）
```bash
# 1. 启动 WebSocket 服务器
cd /Users/czy/Downloads/QGIS/dxf_render_service
python3 -m uvicorn server:app --host 0.0.0.0 --port 8080

# 2. 在浏览器打开
open http://localhost:8080/slave_viewer_map.html
```

### 方式二：通过 master_upload 触发
1. 打开 `master_upload.html`
2. 上传 DXF 文件
3. 数据会自动广播到 `slave_viewer_map.html`

## 坐标系配置

### 如果 DXF 是局部坐标（CAD 坐标）⭐ 最常见情况
1. 在左侧面板选择 **局部坐标 (需配准)**（默认已选）
2. 使用**坐标配准工具**将 DXF 对齐到地图
3. 配准完成后，DXF 会与底图完美叠加

### 如果 DXF 是 WGS84 经纬度
1. 选择 **WGS84 (EPSG:4326)**
2. 图形会直接叠加到底图正确位置（无需配准）

### 如果 DXF 是 CGCS2000
1. 选择对应的 CGCS2000 带号（如 3度带 117E）
2. 会自动转换到 WGS84 显示（无需配准）

### 配准工作流程图
```
上传 DXF ──→ 局部坐标 ──→ 点击配准 ──→ 选DXF特征点 ──→ 选地图对应点
                                           ↓           ↓
                                           └───────────┘
                                                 ↓
                                          重复2-3对点
                                                 ↓
                                          点击「应用」配准
                                                 ↓
                                          DXF 对齐到底图
```

## 天地图 Key 配置

编辑 `slave_viewer_map.html`，替换 YOUR_TIANDITU_KEY：
```javascript
const BASEMAP_CONFIGS = {
    tianditu: {
        url: 'https://t{s}.tianditu.gov.cn/...&tk=你的真实Key',
        ...
    }
};
```

申请地址：https://console.tianditu.gov.cn/

## 架构图

```
┌─────────────────┐     WebSocket      ┌─────────────────┐
│  master_upload  │ ─────────────────→ │  slave_viewer   │
│   (上传 DXF)     │                    │  (纯 DXF 查看)   │
└─────────────────┘                    └─────────────────┘
         │
         │                              ┌─────────────────┐
         └────────────────────────────→ │ slave_viewer_map│
            广播 parsed 消息              │ (DXF + 底图)   │
                                        └─────────────────┘
                                               │
                                               ↓
                                        ┌─────────────────┐
                                        │  高德/天地图/OSM │
                                        │    底图服务      │
                                        └─────────────────┘
```

## 已完成功能 ✅

1. ✅ **坐标配准工具** - 通过 2-3 个控制点将局部坐标转换为 WGS84

## 后续优化建议

2. **PostGIS 集成** - 将 DXF 数据持久化到 PostGIS，支持更大规模数据
3. **QGIS Server** - 发布 WMS 服务，支持更多 GIS 客户端
4. **三维底图** - 集成 Cesium 支持三维地形和建筑
5. **配准参数保存** - 保存配准参数到文件，下次自动应用
6. **批量坐标转换** - 后端支持更多坐标系转换（七参数、四参数）

## 测试步骤

### 基础测试
1. 确保服务器运行：`python3 -m uvicorn server:app --host 0.0.0.0 --port 8080`
2. 打开 `http://localhost:8080/slave_viewer_map.html`
3. 打开 `master_upload.html` 上传 DXF 文件
4. 观察 `slave_viewer_map.html` 是否自动显示 DXF 图形
5. 切换底图类型（高德/天地图/OSM）

### 配准测试 ⭐
1. 上传局部坐标的 DXF 文件
2. 在 `slave_viewer_map.html` 左侧面板确认选择「局部坐标 (需配准)」
3. 点击「🔴 开始配准」按钮
4. 在 DXF 图形上点击一个明显的特征点（如建筑物拐角）
5. 在底图上找到该建筑物的实际位置并点击
6. 重复步骤 4-5，添加第 2 对、第 3 对配准点
7. 观察 DXF 图形是否自动对齐到底图
8. 点击「✓ 应用」完成配准
9. 拖动地图，验证 DXF 与底图是否保持对齐

### 导出测试
1. 配准完成后，点击「⬇ 导出 GeoJSON」或「⬇ 导出 KML」
2. 用 QGIS 或 Google Earth 打开导出的文件，验证坐标是否正确
